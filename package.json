{
  "name": "web-graph-visualizer",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "echo postinstall && node ./node_modules/grunt-cli/bin/grunt build"
  },
  "dependencies": {
    "grunt-cli": "~0.1.13",
    "grunt": "~0.4.2",
    "underscore": "~1.6.0",
    "grunt-bower-task": "~0.3.4"
  },
  "engines": {
    "node": "0.10.x"
  }
}
